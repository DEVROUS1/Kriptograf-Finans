name: kriptograf_finans
environment:
  sdk: '>=3.0.0 <4.0.0'
  
dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  
  # Serialization (kod üretimi ile — null-safe, immutable)
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0
  
  # Chart
  
  # Network
  web_socket_channel: ^2.4.5
  dio: ^5.4.3
  
  # Navigation
  go_router: ^13.2.1
  audioplayers: ^6.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  freezed: ^2.4.7
  json_serializable: ^6.7.1
  riverpod_generator: ^2.3.10
  custom_lint: ^0.6.4
  riverpod_lint: ^2.3.10